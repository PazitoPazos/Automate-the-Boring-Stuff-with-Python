"""
1. Why are functions advantageous to have in your programs?
Because you can have organized your code, don't have repetitive code
and you can use them without know they work internally.

2. When does the code in a function execute: when the function
is defined or when the function is called?
When the function is called.

3. What statement creates a function?
The def statement.

4. What is the difference between a function and a function call?
The function is the miniprogram in itself meanwhile a function call is the
execution of that miniprogram.

5. How many global scopes are there in a Python program? How many local scopes?
There is only one global scope. There is no limit with local scopes.

6. What happens to variables in a local scope when the function call returns?
Local variables will not remember the last values stored in them from the last call.

7. What is a return value? Can a return value be part of an expression?
The value returned by a function or a return statement. Yes, it can be part of an expression.

8. If a function does not have a return statement, what is the return
value of a call to that function?
The result of whatever that code do.

9. How can you force a variable in a function to refer to the global variable?
With the global statement.

10. What is the data type of None?
It is the absence of a value.

11. What does the import areallyourpetsnamederic statement do?
Import a module that have functions.

12. If you had a function named bacon() in a module named spam,
how would you call it after importing spam?
spam.bacon()

13. How can you prevent a program from crashing when it gets an error?
With the try and except statements.

14. What goes in the try clause? What goes in the except clause?
In the try clause the code that could have an error and in
the except clause the code to handle what happens when the error occurs.
"""